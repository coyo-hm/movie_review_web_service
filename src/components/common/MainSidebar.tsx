import styled from '@emotion/styled';

const MainSidebarWrapper = styled.nav`
  grid-area: sidebar;
  background-color: gray;
`;

const MainSidebar = () => {
  return <MainSidebarWrapper></MainSidebarWrapper>;
};
export default MainSidebar;
